# Copy to .env and adjust. Consumed by docker/compose.yml.

# development | test | production
NODE_ENV=development

# Host ports (container ports are fixed at 3000 / 5000)
APP_PORT=3000
SERVER_PORT=5000

# Server log verbosity
LOG_LEVEL=info
